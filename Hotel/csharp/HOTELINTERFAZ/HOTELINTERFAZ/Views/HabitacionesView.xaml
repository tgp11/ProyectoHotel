<Window x:Class="HOTELINTERFAZ.Views.HabitacionesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HOTELINTERFAZ.Views"
        mc:Ignorable="d"
        Title="HabitacionesView" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestión de habitaciones"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Nueva" Width="110" Margin="0,0,8,0"/>
            <Button Content="Editar" Width="110" Margin="0,0,8,0"/>
            <Button Content="Eliminar" Width="110" Margin="0,0,8,0"/>
            <TextBox Width="260"
         Margin="18,0,8,0"
         VerticalContentAlignment="Center" />
            <Button Content="Buscar" Width="110"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nº" Binding="{Binding Numero}" Width="80"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="160"/>
                <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacidad}" Width="120"/>
                <DataGridTextColumn Header="Precio/Noche" Binding="{Binding Precio}" Width="140"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
